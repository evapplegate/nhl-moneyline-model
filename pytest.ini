[pytest]
# Pytest configuration

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage options
addopts = -v --tb=short

# Markers
markers =
    integration: Integration tests
    slow: Slow tests
    unit: Unit tests

